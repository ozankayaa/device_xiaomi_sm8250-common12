allow vendor_cnd default_prop:file { read };

# Allow vendor_cnd to call radio
allow vendor_cnd radio:binder { call };

# Allow vendor_cnd to read, write, open and control IO in vendor_diag_device
allow vendor_cnd vendor_diag_device:chr_file { ioctl open read write };
